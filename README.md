# Ravengard Overlay

test mod
- shows crown values
- shows valuable crown swaps
- press `K` to favourite items so you the mod doesn't flag them

this is probably not bannable

## Build

```bash
export JAVA_HOME=/path/to/jdk-25
./gradlew build
```
# ravengard
